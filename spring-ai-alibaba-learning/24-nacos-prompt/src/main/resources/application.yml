# 本模块自己的配置文件。
# 关键点：通过 spring.config.import 导入“项目唯一的共享配置文件”。
spring:
  main:
    web-application-type: none
  application:
    name: 24-nacos-prompt
  config:
    import:
      - "optional:file:../config/spring-ai-alibaba-common.yml"
      - "optional:file:./config/spring-ai-alibaba-common.yml"
      - "optional:file:spring-ai-alibaba-learning/config/spring-ai-alibaba-common.yml"

# =============================================================================
# ★ Nacos 连接配置（默认全部注释掉，保证没有 Nacos 环境也能启动本模块做演示）★
# 有 Nacos 时：去掉下面注释、填好地址，并在 Nacos 里创建对应的 Prompt 配置（见 README）。
# =============================================================================
#spring:
#  ai:
#    alibaba:
#      nacos:
#        prompt:
#          # 是否启用 Nacos 动态 Prompt
#          enabled: true
#      # Nacos 服务器地址与命名空间/分组
#  nacos:
#    config:
#      server-addr: 127.0.0.1:8848      # Nacos 服务器地址
#      namespace: public                # 命名空间
#      group: DEFAULT_GROUP             # 配置分组
#      username: nacos                  # 若开启鉴权
#      password: nacos
